# LLM routing (used by agents/llm.py)
litellm

# MCP Server Framework (standalone package; FastMCP is no longer bundled in mcp v1+)
fastmcp>=2.0.0,<3.0.0

# Vector Database & Embeddings
qdrant-client>=1.10.0
cohere>=5.0.0

# Chunking & Data Utilities
langchain-text-splitters>=0.2.0
pydantic>=2.7.0
python-dotenv>=1.0.1

# Web server
fastapi[standard]
uvicorn[standard]
python-multipart

# File parsing (used by pipeline.py)
pypdf
python-docx